language: python
python:
  - "3.4"
branches:
    only:
        - master
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install python-coveralls
  - pip install coverage
# command to run tests
script:
    - pylint app.py
    - coverage run test_app.py test
after_success:
    - coveralls
